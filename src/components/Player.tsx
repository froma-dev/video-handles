import MediaPlayer from '@components/MediaPlayer.tsx'
import ShakaMediaPlayer from '@components/ShakaPlayer.tsx'

type MediaPlayers = typeof ShakaMediaPlayer | typeof MediaPlayer

const Player = ({}) => {

}

export default Player